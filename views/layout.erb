<!DOCTYPE html>
<html>
	<head>
		<meta http-equiv="Content-Type" content="text/html; charset=utf-8">
		<title>mdweb: <%= path_to_title path %></title>
		<link rel="stylesheet" href="/style.css">
	</head>

	<body>
		<% path = path.sub('./data', '').split('/').reject { |p| p == '' || p == '.' } %>
		<div id="crumb">
			<a href="/">[root]</a>
			<% path.each_with_index do |p, i| %>
				/
				<%= "<a href='/#{e path[0..i].join('/')}'>#{e p}</a>" %>
			<% end %>
		</div>

		<%= yield %>
	</body>
</html>
